# Полный анализ сайта ANRO TRIP (White Anrotrip)

**Дата анализа:** 2026
**Охват:** все страницы (главная, `/corp`, `/cabinet`), desktop + mobile, шрифты, изображения, переносы, адаптивность, скорость, UX, UI, SEO, управление с клавиатуры, Lighthouse (27.02.2026), тесты по веб-стандартам.

---

## Часть 1. Общий анализ сайта (все страницы)

### 1. Шрифты

#### Положительно

- **Preconnect** для Google Fonts, `display=swap` и `media="print" onload="this.media='all'"` — снижают блокировку рендера.
- Используются **Inter** (400, 500, 600) и **Montserrat** (600, 700, 800).
- `text-rendering: optimizeLegibility`, `-webkit-font-smoothing: antialiased`.
- **Fluid typography** через `clamp()` для h1–h6 без жёстких пикселей.
- В `html`: `font-size: clamp(14px, 0.875rem + 0.25vw, 16px)` — адаптивная база.

#### Проблемы

- **Fallback:** нет явного системного fallback при недоступности Google Fonts (например, `font-family: 'Montserrat', system-ui, sans-serif`).
- **FOUT/FOIT:** при `display=swap` возможны кратковременные скачки вёрстки при подгрузке шрифтов.
- На мобильных часть заголовков с очень крупным `clamp()` (например, Hero: `2.8rem … 8rem`) может быть избыточно крупной на узких экранах.

---

### 2. Изображения

#### Положительно

- Компонент **`<Image />`** из `astro:assets` — оптимизация, lazy loading.
- Hero: **AVIF**, `quality={75}`, `loading="eager"`, `fetchpriority="high"`.
- Туры, награды, партнёры — **WebP**.
- Для фонов и карточек используется `object-cover`.
- Класс `.image-sharp` с `translateZ(0)` и `backface-visibility: hidden` для стабильного рендеринга.

#### Проблемы

- **Team:** смесь WebP, PNG, JPG — PNG/JPG тяжелее и хуже для скорости.
- **Reviews:** локальный placeholder `/images/review-placeholder.svg` — до деплоя заменить на реальные фото; сейчас не placehold.co.
- **GiftModal:** `<img src="/images/gift-card-preview.jpg">` вместо `<Image />` — без оптимизации Astro.
- Явные `width`/`height` и `sizes` для критичных изображений не проверялись — возможен layout shift.
- Hero с `scale-105` и kenburns может давать артефакты на слабых мобильных GPU.

---

### 3. Переносы строк

#### Положительно

- Используются управляемые переносы через `<br />` и `hidden sm:block` (Hero, GiftSection и др.).
- Пример: «до<br class="hidden sm:block" /> полного» — разный перенос на мобиле и десктопе.

#### Проблемы

- Длинные строки без управления переносами в About, TrustBadges, Footer.
- Нет `word-break` / `overflow-wrap` для длинных слов (URL, ФИО, реквизиты).
- В CallbackModal: «Оставьте свои контакты,<br />и мы свяжемся...» — на мобиле возможен неудобный разрыв.
- В Reviews и Team используется **truncate** без `title` — обрезанный текст без подсказки.
- В TrustBadges: `sm:whitespace-nowrap` — на очень узких экранах текст может вылезать за границы.

---

### 4. Адаптивность (Desktop + Mobile)

#### Положительно

- Единая сетка: `px-4 sm:px-6`, `max-w-7xl`, `container mx-auto`.
- Брейкпоинты: `sm`, `md`, `lg`, `xl`.
- Hero: `min-h-dvh` — учёт высоты viewport на мобильных.
- Header: бургер на `lg`, навигация переключается по ширине.
- Partners: marquee на desktop, grid на mobile.
- Kenburns, gradient text, breathe-анимации отключены на touch через `@media (hover: hover)`.
- Для Tourvisor: `-webkit-overflow-scrolling: touch`.
- Карусель Reviews: `snap-x snap-mandatory`.
- Мобильное меню: `fixed top-[80px] right-4 w-56`.

#### Проблемы

- Header: логотип с `text-[8px]`, `text-[7px]` — очень мелко на мобиле.
- Hero подзаголовок: `max-w-xl` — на узких экранах может быть тесно.
- TrustBadges: `grid-cols-2 sm:grid-cols-3` — на мобиле две колонки, длинные значения могут не помещаться.
- Footer: на мобиле реквизиты в `text-xs` — читаемость под вопросом.
- Нет явной проверки на очень малые ширины (320px и меньше).
- Modal/CookieBanner с `max-h-[90vh]` — на малых экранах с виртуальной клавиатурой возможны проблемы.

---

### 5. Скорость

#### Положительно

- Astro — статический рендер, минимум клиентского JS.
- Подключены sitemap и compressor.
- Lazy loading для большинства изображений.
- Hero: `loading="eager"`, `fetchpriority="high"`.
- Учтён `prefers-reduced-motion` — анимации отключаются.
- Blur и тяжёлые анимации только при hover — экономия на мобиле.

#### Проблемы

- Шрифты с Google Fonts — дополнительный DNS и запросы.
- Tourvisor/Nemo — тяжёлые виджеты/iframe, основной риск для LCP/TTI.
- View Transitions (ClientRouter) — дополнительный JS.
- Hero: AVIF без явного fallback для старых браузеров.
- Reviews: внешние изображения — лишние запросы.
- Не проверялись: preload для LCP-картинки, размер бандла, количество запросов.

---

### 6. UX

#### Положительно

- Якорные ссылки: `#search`, `#about`, `#reviews` и др.
- `scroll-margin-top` для учёта фиксированного header.
- Плавный скролл к секциям (`behavior: 'smooth'`).
- Мобильное меню закрывается по клику вне области.
- При открытых модалках блокируется скролл с компенсацией ширины скроллбара.
- Есть `aria-label`, `aria-expanded` на кнопках.
- Focus visible: `focus-visible:ring-2`.
- Touch targets в основном ≥ 44px.
- Учтён `prefers-reduced-motion`.

#### Проблемы

- Логотип: сложное поведение (fade-out + навигация) — может быть неочевидным.
- PopularTours: клик по карточке без явного действия (модалка/переход) — неочевидно для пользователя.
- Reviews: `openLightbox(index)` через глобальную функцию в `onclick` — хрупко.
- CookieBanner: поведение и доступность не проверялись.
- Ссылки «Политика конфиденциальности» / «Пользовательское соглашение» ведут на `#` — заглушки.
- FavoritesWidget, OfficeWidget: инициализация по `astro:page-load` — нужно убедиться, что работают после навигации.
- GiftModal: вызов `openGiftModal()` — проверить наличие и вызов.

---

### 7. UI

#### Положительно

- Единая палитра: primary, secondary, cta.
- Glassmorphism, скругления rounded-3xl, консистентные отступы.
- Scroll reveal, stagger-анимации.
- Bento grid в PopularTours и About.
- Единый паттерн заголовков: бейдж + h2 + описание.

#### Проблемы

- Awards: ранее были конфликты transition при hover — стоит убедиться в стабильности.
- Partners: класс `partners-shine` — проверить наличие в стилях.
- Тёмные секции (Reviews, Team, GiftSection): `text-white/40`, `text-white/35` — контраст может не соответствовать WCAG AA.
- TrustBadges: `text-[10px]` — очень мелкий шрифт.
- Крупные z-index (до `z-2147483647`) — возможны конфликты слоёв.

---

### 8. SEO

#### Положительно

- `<html lang="ru">`.
- Meta: viewport, description, theme-color.
- Open Graph: type, title, description, locale.
- Динамические `title` и `description` в Layout.
- Favicon, apple-touch-icon.
- Sitemap через `@astrojs/sitemap`.

#### Проблемы

- Нет **og:image** — превью в соцсетях будет по умолчанию или пустое.
- Нет **og:url**.
- Нет **canonical URL**.
- В репозитории не найден **robots.txt**.
- Нет **Schema.org** (TravelAgency, LocalBusiness, FAQ и т.п.).
- Нет **breadcrumbs**.
- Структура H2–H3 не проверялась детально.
- Alt у изображений есть, но информативность не оценивалась.
- Reviews: локальный placeholder — заменить на реальные фото до продакшена; для SEO нужен реальный контент.

---

### 9. Управление с клавиатуры (Keyboard control)

#### Положительно

- Есть `aria-label`, `aria-expanded` на кнопках (Header, corp Hero, Footer).
- `focus-visible:ring-2` на TrustBadges — видимый фокус при навигации Tab.
- Поля форм (CallbackModal, GiftModal, ReviewModal, Reviews, cabinet) с `focus:ring`, `focus:border-primary` — фокус заметен.
- Header: кнопка меню с `aria-label="Меню"`, `aria-expanded` переключается при открытии/закрытии.
- Скролл-стрелки Hero/corp: `aria-label="Скролл вниз"` / `"Прокрутить вниз"`.
- Соцсети в Footer: `aria-label` на ссылках (VK, WhatsApp, Telegram).
- Табы SearchWidget: семантически кнопки — доступны по Tab; переключение по клику (без клавиш — см. проблемы).

#### Проблемы

- ~~**Нет Skip Link**~~ ✅ Добавлено в Layout.astro — «Перейти к содержимому» видна при фокусе.
- **Модальные окна (CallbackModal, GiftModal, ReviewModal, lightbox в Reviews):**
  - Escape для закрытия — не проверялось, возможно отсутствует.
  - Focus trap — при открытии фокус должен уходить внутрь модалки и не выходить за её пределы; при закрытии — возврат к элементу, открывшему модалку.
  - Фокус на первом интерактивном элементе при открытии.
- **Табы SearchWidget:** нет навигации стрелками (← →) и Home/End; нужны `role="tablist"`, `role="tab"`, `role="tabpanel"`, `aria-selected`, `tabindex` для roving tabindex.
- ~~**Lightbox Reviews:** Escape, стрелки~~ ✅ Реализовано (Escape, стрелки, focus trap, возврат фокуса).
- **Карусель Reviews:** горизонтальный скролл — Tab даёт доступ к карточкам, но навигация стрелками внутри карусели не проверялась.
- **Бургер-меню Header:** Enter/Space активируют кнопку (нативно), но закрытие по Escape — не проверялось.
- **PopularTours:** карточки — `<button>`; Enter/Space работают, но фокус на карточках и переход к модалке при нажатии Enter — не проверялось.
- **Однотипность `focus-visible`:** TrustBadges имеет `focus-visible:ring-2`, у многих кнопок и ссылок только `focus:outline-none` — видимый фокус может пропадать; лучше везде использовать `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2`.
- **tabindex="-1"** на интерактивных элементах, которые должны быть вне Tab-порядка (например, декоративные) — не проверялось; `tabindex="0"` на кастомных виджетах при необходимости.

#### Рекомендации по оптимизации клавиатурного управления

| Приоритет      | Действие                                                                                                              |
| -------------- | --------------------------------------------------------------------------------------------------------------------- |
| ~~Высокая~~    | ~~Skip Link~~ ✅ Сделано.                                                                                             |
| ~~Высокая~~    | ~~Модалки: Escape, focus trap, возврат фокуса~~ ✅ Сделано (CallbackModal, GiftModal, ReviewModal, lightbox Reviews). |
| **Высокая**    | Унифицировать `focus-visible:ring-2` для всех интерактивных элементов (кнопки, ссылки, карточки-кнопки).              |
| **Средняя**    | SearchWidget табы: `role="tablist"`, `role="tab"`, `role="tabpanel"`; Arrow Left/Right, Home/End; roving tabindex.    |
| ~~Средняя~~    | ~~Lightbox Reviews~~ ✅ Сделано (Escape, стрелки, focus trap).                                                        |
| **Средняя**    | Бургер-меню: закрытие по Escape.                                                                                      |
| **Желательно** | Карусель Reviews: поддержка стрелок для навигации по карточкам (если это улучшит UX).                                 |
| **Желательно** | Проверить Tab-порядок на всех страницах — логичный поток без «прыжков».                                               |

---

### Резюме по приоритетам (общий анализ)

| Категория        | Критично                             | Важно                                                  | Желательно                         |
| ---------------- | ------------------------------------ | ------------------------------------------------------ | ---------------------------------- |
| **Шрифты**       | —                                    | Fallback, FOUT                                         | Проверка clamp на мобиле           |
| **Изображения**  | Placeholder Reviews, img в GiftModal | Конвертация Team в WebP                                | width/height, sizes                |
| **Переносы**     | —                                    | overflow-wrap, truncate + title                        | Управление переносами              |
| **Адаптивность** | —                                    | Header, Footer на мобиле                               | Проверка 320px                     |
| **Скорость**     | Влияние Tourvisor/Nemo               | Preload LCP, шрифты                                    | Аудит бандла                       |
| **UX**           | —                                    | Действия PopularTours, ссылки Footer                   | Логика логотипа                    |
| **UI**           | —                                    | Контраст тёмных секций                                 | Проверка hover/transition          |
| **SEO**          | og:image                             | Schema.org, canonical, robots                          | Breadcrumbs, FAQ                   |
| **Клавиатура**   | —                                    | Skip Link, Escape/focus trap в модалках, focus-visible | Табы/стрелки, проверка Tab-порядка |

---

## Часть 2. Полный анализ страницы CORP (`/corp`)

### 2.1. Мобильное отображение текста — основные проблемы

#### 2.1.1. Длинные строки без переноса

**Списки (блоки 3 «Авиабилеты», 5 «Туры», 6 «Трансфер»):**

- ~~Разметка: `<li class="flex gap-3">` без min-w-0~~ ✅ Добавлено `min-w-0 break-words` на текст в списках блоков 3, 5, 6.

**Длинные CAPS-фразы (риск переполнения на мобиле):**

- «СОБСТВЕННАЯ АВИАКАССА»
- «ОФОРМИТЬ, ПОМЕНЯТЬ, ВЕРНУТЬ»
- «БОНУСНЫХ ПРОГРАММАХ»
- «ЛИЦЕНЗИЮ ТУРОПЕРАТОРА (РТО 022708)»
- «МЫ СОТРУДНИЧАЕМ С НАДЕЖНЫМИ ТУРОПЕРАТОРАМИ»
- «ОНЛАЙН ПОИСКОМ ТУРОВ НА НАШЕМ САЙТЕ»
- «WWW.ANROTRIP.RU»
- «ВИЗОВУЮ ПОДДЕРЖКУ»

CAPS в сочетании с `tracking` дают очень широкие строки — на мобиле легко выход за границы.

#### 2.1.2. Переносы строк

| Место                                                             | Проблема                                                              |
| ----------------------------------------------------------------- | --------------------------------------------------------------------- |
| «Коммерческое<br />предложение»                                   | Жёсткий разрыв; на очень узких экранах оба слова длинные.             |
| «Профессиональный сервис —<br /><span>ведущее направление</span>» | Вторая строка короткая — возможен «висящий» блок.                     |
| «Организуем командировку, поездку на конференцию...»              | Одна длинная строка без адаптивного `<br class="hidden sm:block" />`. |

#### 2.1.3. Размеры шрифтов

- Заголовки: clamp-ы в целом адекватны (H1 ~2.5rem на малых, H2/H3 меньше).
- Бейджи и теги: `text-xs` (12px) — на мобиле на грани читаемости.
- Карточки преимуществ: `text-sm` (14px).
- Подпись к WhatsApp в контактах: `text-white/40 text-sm`.

#### 2.1.4. Кнопки Hero

- Четыре CTA в ряд: Связаться, Mail, Phone, WhatsApp.
- «WhatsApp» длиннее остальных.
- На 320px кнопки сжимаются — текст может переноситься или выглядеть сжатым.

#### 2.1.5. Блок контактов

- «(WhatsApp, Viber, Telegram)», «(представительство)» в одну строку — на узком экране может не помещаться.
- Нет `word-break` / `overflow-wrap` — риск overflow.

---

### 2.2. Изображения (corp)

#### Положительно

- Все изображения через `<Image />`, формат WebP.
- `object-cover`, `object-top`.
- Hero: `loading="eager"`, `fetchpriority="high"`.

#### Замечания

- Фиксированная высота блоков: `h-[280px] md:h-[360px]` — на очень узких экранах пропорции могут быть неидеальны.
- Блок corp4: `h-[480px] md:h-[720px]` — на мобиле 480px по высоте занимает много экрана.

---

### 2.3. Адаптивность (corp)

#### Положительно

- Сетка: `grid-cols-1 lg:grid-cols-[...]` — одна колонка на мобиле.
- `order-2 lg:order-1` / `order-1 lg:order-2` — логичная смена порядка фото/текста.
- Отступы: `px-4 sm:px-6`, `gap-10 lg:gap-16`.

#### Замечания

- Контентный блок: `mt-[20vh]` — как на главной; на landscape-мобиле Hero маленький, 20vh может «съедать» много экрана.
- Якорь «О компании» ведёт на `#content` — на corp это первый контентный блок, а не блок «О нас»; при необходимости уточнить формулировку или якорь.

---

### 2.4. Переносы и типографика (corp)

- Жёсткие `<br />` в H1 и H2 без адаптивных вариантов (как на главной).
- Длинные абзацы без `hyphens` или управляемых переносов.
- Списки с `flex gap-3`: текст без `min-w-0` и без `overflow-wrap`/`word-break`.

---

### 2.5. UX (corp)

#### Положительно

- Якорная навигация в header по секциям corp.
- CTA: Связаться, Mail, Phone, WhatsApp.
- Скролл к `#content`.
- `aria-label` на кнопках.

#### Замечания

- ~~Нет `scroll-margin-top`~~ ✅ Добавлено в global.css для `#content`, `#corp-avia`, `#corp-hotels` и др.
- Много CTA в Hero — возможное рассеивание внимания.

---

### 2.6. UI (corp)

- Единый стиль с главной: бейджи, заголовки, карточки.
- Чередование фона: `bg-gray-50` / `bg-white`.
- Контраст в блоке контактов: `text-white/70`, `text-white/40` — проверить на WCAG AA.

---

### 2.7. SEO (corp)

- Отдельные `title` и `description` в Layout.
- Один H1, иерархия H2/H3 соблюдена.
- Alt у изображений заданы.

Нет: отдельная Schema.org для страницы, canonical для `/corp`, og:image для этой страницы.

---

### 2.8. Скорость (corp)

- WebP, оптимизация через Astro.
- Hero: `loading="eager"`, `fetchpriority="high"`.
- Остальные изображения — lazy.
- Нет дополнительного тяжёлого JS на странице.

---

### 2.9. Резюме приоритетов по CORP (мобильный текст)

| #   | Проблема                                                    | Где          | Важность |
| --- | ----------------------------------------------------------- | ------------ | -------- |
| 1   | ~~Длинные CAPS и фразы~~ ✅ Сделано (`min-w-0 break-words`) | —            | —        |
| 2   | ~~`flex gap-3` без min-w-0~~ ✅ Сделано                     | —            | —        |
| 3   | ~~overflow-wrap~~ ✅ break-words добавлен                   | —            | —        |
| 4   | Четыре CTA в Hero — тесно на мобиле                         | Hero         | Средняя  |
| 5   | Мелкий `text-xs` в бейджах/тегах                            | Бейджи, теги | Средняя  |
| 6   | Нет адаптивных `<br />` в заголовках                        | H1, H2       | Низкая   |
| 7   | ~~Нет scroll-margin-top~~ ✅ Сделано                        | —            | —        |

---

## Часть 2.5. Анализ страницы Cabinet (`/cabinet`)

Страница входа в личный кабинет туриста. Форма отправляется на внешний хост `lk.anrotrip.ru`. Виджеты (ScrollProgress, ScrollToTop, FavoritesWidget, OfficeWidget) отключены.

### 2.5.1. SEO

#### Положительно

- Указаны `title` и `description` в Layout.
- Единственный H1: «Личный кабинет туриста».

#### Проблемы

- Нет `robots noindex` — страница входа может индексироваться; для форм логина обычно используют `noindex, nofollow`.
- Нет canonical для `/cabinet`.
- Изображение: `alt=""` — допустимо для декоративного фона, но можно добавить описательный alt для доступности.

### 2.5.2. UX

#### Положительно

- Один чёткий сценарий: вход в ЛК.
- Форма компактная, `max-w-[400px]`.
- Радио Телефон/Email — переключение подсказки для поля ввода.
- `autocomplete="username"` на поле ввода.

#### Проблемы

- Нет Header — нет навигации и возврата на главную без кнопки «Назад».
- Пустой `placeholder=""` — при пустом поле неочевидно, что вводить (телефон или email).
- `input` всегда `type="text"` — при выборе «Телефон» предпочтителен `type="tel"`, при «Email» — `type="email"`.
- Нет ссылок «Забыли пароль?» / «Регистрация» — может быть намеренно, если это внешний ЛК.
- `main class="fixed inset-0"` — страница не скроллится; на мобиле с клавиатурой форма может уезжать вверх или перекрываться.
- Блок внизу (© + ссылка moidokumenti.ru) — на мобиле может перекрываться или плохо читаться.
- Отправка на внешний URL — нет индикатора загрузки и явного уведомления о переходе.

### 2.5.3. UI

#### Положительно

- Карточка формы: `bg-white/85`, тень — читаемость на фоне.
- Подпись внизу: `text-shadow` — читаемость на фоне фото.
- Кнопка «Войти» с достаточным размером.

#### Проблемы

- Нет `focus-visible:ring-2` на кнопке «Войти» — единый видимый фокус для интерактивных элементов.
- Ссылка moidokumenti.ru идёт сразу после © в том же блоке — на мобиле кликабельная область может быть неудобной.

### 2.5.4. Управление с клавиатуры

#### Положительно

- Поля формы доступны по Tab; radio, input и кнопка — в логичном порядке.
- `autocomplete="username"`.

#### Проблемы

- Радио-группа без `<fieldset>` и `<legend>` — хуже для скринридеров.
- Нет `aria-label` у radio-группы и поля ввода.
- Кнопка «Войти» без явного `focus-visible` — фокус может быть незаметен.

### 2.5.5. Резюме приоритетов по Cabinet

| #   | Проблема                                  | Где            | Важность |
| --- | ----------------------------------------- | -------------- | -------- |
| 1   | Нет Header — нет возврата на главную      | Вся страница   | Высокая  |
| 2   | Пустой placeholder, type всегда text      | Поле ввода     | Высокая  |
| 3   | Нет noindex для страницы входа            | SEO            | Средняя  |
| 4   | Нет fieldset/legend у radio-группы        | Форма          | Средняя  |
| 5   | fixed inset-0 — риск перекрытия на мобиле | Layout         | Средняя  |
| 6   | Нет focus-visible на кнопке               | UI/доступность | Средняя  |

---

## Часть 3. Выводы по трендам 2026 (из dev_site.md) и сводные рекомендации

Источник: `.doc/dev_site.md` — советы по разработке сайта в 2026 году (дизайн, UX, SEO, GEO, AEO). Ниже — выводы из этого документа, сопоставленные с текущим состоянием сайта и объединённые с рекомендациями анализа.

### 3.1. Дизайн и UX: бизнес-задача и mobile first

**Из dev_site.md:**

- Дизайн и UX должны решать бизнес-задачу; структура по логике: проблема → решение → доказательства → условия → действие.
- **Mobile first без компромиссов** (64%+ трафика на мобильные): контент мобильной версии должен совпадать с десктопной, меню не должно прятать ключевые разделы, кнопки и формы — удобны для смартфонов.
- Скорость — часть UX и SEO: лёгкие шрифты и изображения (WebP/AVIF), минимум тяжёлых скриптов на первом экране, без «прыжков» интерфейса.
- Ясность: понятные заголовки, один главный сценарий на странице, тексты для быстрого сканирования, контраст и кликабельные зоны.

**Выводы по сайту ANRO TRIP:**

- Структура главной и corp в целом соответствует схеме (Hero → контент → доказательства → контакты/CTA).
- Мобильная версия по контенту не урезана, но **текст на мобиле отображается некорректно** (corp: длинные строки, CAPS, списки без переноса) — это противоречит требованию «мобильная версия не уступает».
- Скорость частично заложена (Astro, WebP/AVIF, lazy load), но Tourvisor/Nemo и шрифты — риски; предсказуемость отступов/размеров в целом есть, риски — layout shift и overflow текста.
- **Рекомендация:** закрыть проблемы мобильного текста (overflow-wrap, min-w-0, при необходимости адаптивные переносы), проверить один главный сценарий на главной и на corp (не размывать четырьмя CTA в Hero corp).

---

### 3.2. SEO: структура и семантика до дизайна, E-E-A-T, микроразметка

**Из dev_site.md:**

- SEO закладывать до/вместе с дизайном: карта страниц под спрос (услуги, направления, «О компании», «Контакты», кейсы, FAQ, лендинги под рекламу).
- Контент и доверие: **E-E-A-T** (опыт, экспертность, авторитет, доверие) — авторство, факты, цифры, кейсы, команда, сертификаты, актуальность.
- **Микроразметка Schema.org** — не «для галочки», а для понимания контента и богатых сниппетов (рейтинги, контакты, отзывы и т.д.).

**Выводы по сайту ANRO TRIP:**

- Карта страниц минимальная, но логичная: главная (услуги, туры, о нас, отзывы, награды, команда, контакты) + corp (КП).
- E-E-A-T частично отражён: команда, награды, отзывы, партнёры, TrustBadges; но нет явного авторства статей, FAQ нет, Reviews на placeholder — не работают как доказательства.
- **Schema.org отсутствует** — расхождение с трендом 2026 и рекомендацией dev_site.
- **Рекомендация:** добавить Schema.org (LocalBusiness/TravelAgency, при необходимости FAQPage, контакты, отзывы); заменить placeholder в Reviews на реальный контент; рассмотреть блок FAQ на главной или corp; добавить og:image, canonical, robots.txt.

---

### 3.3. GEO и AEO: ответы на вопросы и «машинная читаемость»

**Из dev_site.md:**

- Сайт должен давать чёткие ответы и выглядеть надёжным для генеративных и ответных систем.
- Тексты: короткие определения в начале блока, списки, чеклисты, блок «вопрос → короткий ответ → детализация», единые термины.
- Усиление «машинной читаемости»: микроразметка Schema.org, заголовки H2–H3 по смыслу запросов, отдельные страницы под темы, блоки FAQ.

**Выводы по сайту ANRO TRIP:**

- Много текста в формате «описание + списки» (About, corp) — подходит под формат «ответ + детализация».
- Отдельных страниц под направления/услуги нет; одного блока FAQ нет — потенциал для GEO/AEO не используется.
- **Рекомендация:** ввести блок FAQ (хотя бы на главной или corp) с чёткими «вопрос — короткий ответ»; сохранять единые термины (ANRO TRIP, корпоративное обслуживание и т.д.); при добавлении Schema.org учитывать FAQ и контакты для сниппетов и цитирования.

---

### 3.4. Интеграции и аналитика: «измеряйте не трафик, а деньги»

**Из dev_site.md:**

- Интеграция с CRM (заявки, звонки, чаты, формы), события и цели (форма, клик по телефону и т.д.), сквозные метки, дашборд по лидам и конверсии.

**Выводы по сайту ANRO TRIP:**

- В анализе не проверялись: наличие CRM, цели в аналитике, сквозная аналитика. Сайт даёт точки контакта (телефон, почта, WhatsApp, формы обратного звонка и отзыва).
- **Рекомендация:** зафиксировать, что считается главной конверсией (звонок, заявка, переход в мессенджер); настроить цели и при необходимости интеграцию с CRM; при добавлении/изменении форм — сразу закладывать события и метки.

---

### 3.5. Итог 2026: «сайт — система продаж и доверия»

**Из dev_site.md:**

- Сайт должен: быстро давать ответы и вести к действию; быть понятен поиску (структура + микроразметка); соответствовать mobile-first и качеству опыта; помогать GEO/AEO выбирать источник; измерять результат в лидах и выручке.

**Выводы по сайту ANRO TRIP:**

- Сильные стороны: понятная структура, CTA, контакты, доверие (награды, команда, партнёры), быстрая база (Astro, оптимизация изображений).
- Пробелы: мобильное отображение текста (особенно corp), отсутствие Schema.org и FAQ, риски скорости (виджеты, шрифты), заглушки в Footer, placeholder в Reviews.
- Если закрыть SEO, GEO/AEO и мобильный текст «на старте», снижается риск переделок и потери части мобильной аудитории и доверия поиска.

---

### 3.6. Сводный список действий (анализ + dev_site.md)

Объединённые приоритеты с учётом Части 1, Части 2 и выводов из dev_site.md:

| Приоритет      | Действие                                                                                                                                                                                                   |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~Критично~~   | ~~Мобильный текст на corp~~ ✅ Сделано (break-words, min-w-0 в списках).                                                                                                                                   |
| **Критично**   | Заменить placeholder изображений в Reviews на реальные; GiftModal — перевести на `<Image />` или оптимизировать.                                                                                           |
| **Критично**   | Добавить og:image (хотя бы для главной и corp).                                                                                                                                                            |
| **Важно**      | Внедрить Schema.org (LocalBusiness/TravelAgency, контакты; при появлении FAQ — FAQPage).                                                                                                                   |
| **Важно**      | Добавить canonical URL, og:url; при необходимости robots.txt.                                                                                                                                              |
| ~~Важно~~      | ~~scroll-margin-top для corp~~ ✅ Сделано. Мобиль: проверить Header/Footer (размер шрифтов, читаемость).                                                                                                   |
| **Важно**      | Один главный сценарий: уточнить действие при клике на карточки PopularTours; на corp — не размывать Hero четырьмя равнозначными CTA.                                                                       |
| **Важно**      | Контраст в тёмных секциях (Reviews, Team, GiftSection, corp-контакты) — проверить по WCAG AA.                                                                                                              |
| **Важно**      | **Клавиатура:** Skip Link, Escape и focus trap в модалках, унифицировать `focus-visible:ring-2` для интерактивных элементов.                                                                               |
| **Желательно** | Блок FAQ (главная или corp) — для пользователей и GEO/AEO.                                                                                                                                                 |
| **Желательно** | Fallback шрифтов, preload LCP, аудит влияния Tourvisor/Nemo на скорость.                                                                                                                                   |
| **Желательно** | Аналитика и цели: зафиксировать главную конверсию, при необходимости CRM и сквозные метки.                                                                                                                 |
| **Важно**      | **Cabinet:** добавить Header (или ссылку «На главную»); placeholder и type для поля ввода в зависимости от radio; `noindex` для страницы входа; fieldset/legend для radio-группы; focus-visible на кнопке. |

---

## Часть 4. Lighthouse — автоматический аудит (27.02.2026)

**Условия:** главная страница, mobile-режим, preview (localhost:4321), Lighthouse 13.0.3.

### 4.1. Оценки по категориям

| Категория          | Оценка | Комментарий                  |
| ------------------ | ------ | ---------------------------- |
| **SEO**            | 100    | Отлично                      |
| **Accessibility**  | 88     | Хорошо                       |
| **Performance**    | 68     | Есть резервы для оптимизации |
| **Best Practices** | 58     | Есть замечания               |

### 4.2. Выявленные проблемы

#### Performance (68)

- **Reduce unused CSS** — ориентировочно 213 KiB неиспользуемого CSS; возможны пути оптимизации (critical CSS, tree-shaking).
- **Reduce unused JavaScript** — ориентировочно 890 KiB неиспользуемого JS; вероятно связано с Tourvisor/Nemo и другими виджетами; рассмотреть отложенную загрузку.

#### Best Practices (58)

- **Does not use HTTPS** — ожидаемо на localhost; на проде с HTTPS снимается.
- **Browser errors were logged to the console** — есть ошибки в консоли; нужно проверить и исправить.
- **Uses deprecated APIs** — используются устаревшие API; уточнить, какие именно, и по возможности заменить.

#### Accessibility (88)

- Общий уровень хороший; детали — в полном Lighthouse-отчёте (HTML).

### 4.3. Положительные результаты Lighthouse

- SEO: 100 — структура, meta, семантика в целом в порядке.
- Accessibility: 88 — базовая доступность на хорошем уровне.

### 4.4. Рекомендации по результатам Lighthouse

| Приоритет      | Действие                                                                                      |
| -------------- | --------------------------------------------------------------------------------------------- |
| **Важно**      | Сократить неиспользуемый CSS (critical CSS, tree-shaking Tailwind, исключение лишних стилей). |
| **Важно**      | Сократить неиспользуемый JS (lazy load Tourvisor/Nemo, код-сплиттинг).                        |
| **Важно**      | Исправить ошибки в консоли браузера.                                                          |
| **Важно**      | Выявить и обновить устаревшие API.                                                            |
| **Желательно** | Повторить Lighthouse после деплоя на HTTPS — часть предупреждений Best Practices снимется.    |
| **Желательно** | Повторить Lighthouse для страниц corp и cabinet — возможно, дополнительные находки.           |

### 4.5. Файл отчёта

Полный отчёт сохранён: `lighthouse-report.html` (в корне проекта). Открыть в браузере для детального просмотра всех аудитов и рекомендаций.

---

## Часть 5. Сводка: что сделано и что осталось (по приоритетам)

### 5.1. Что сделано ✅

| Область                         | Сделано                                                                                                                                                                                                   |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Skip Link**                   | Ссылка «Перейти к содержимому» в Layout.astro, видима при фокусе.                                                                                                                                         |
| **Модалки**                     | Escape для закрытия, focus trap, возврат фокуса — CallbackModal, GiftModal, ReviewModal, lightbox Reviews.                                                                                                |
| **Corp: мобильный текст**       | `break-words` во всех абзацах; `min-w-0 break-words` в списках блоков 3, 5, 6.                                                                                                                            |
| **Corp: scroll-margin-top**     | Якоря `#content`, `#corp-avia`, `#corp-hotels` и др. в global.css.                                                                                                                                        |
| **Corp: контент при скролле**   | Блок контента `-translate-y-[20vh] mt-0` — появляется сразу при первом скролле.                                                                                                                           |
| **Производительность (mobile)** | `blur-0 md:blur-3xl` на орбах; `glass-panel-mobile-solid` для TrustBadges и Partners stats; `transition-all` → конкретные свойства в карточках; `-webkit-overflow-scrolling: touch` в Reviews и Partners. |
| **Типографика**                 | `text-wrap: pretty`; `&nbsp;` в Hero и corp Hero для предлогов.                                                                                                                                           |
| **About: перенос**              | `<br class="md:hidden" />` перед «довольных клиентов».                                                                                                                                                    |
| **Header**                      | Бургер `col-start-3` — на mobile всегда справа.                                                                                                                                                           |

### 5.2. Что осталось (по приоритетам)

#### Критично (перед продакшеном)

| #   | Задача                                                         | Где              |
| --- | -------------------------------------------------------------- | ---------------- |
| 1   | Добавить **og:image** (хотя бы главная, corp)                  | Layout, страницы |
| 2   | Заменить placeholder в Reviews на реальные фото                | Reviews.astro    |
| 3   | GiftModal: перевести `<img>` на `<Image />` или оптимизировать | GiftModal.astro  |

#### Важно

| #   | Задача                                                                            | Где                    |
| --- | --------------------------------------------------------------------------------- | ---------------------- |
| 4   | **Schema.org** (LocalBusiness/TravelAgency, контакты)                             | Layout, JSON-LD        |
| 5   | **canonical URL**, og:url                                                         | Layout                 |
| 6   | **robots.txt**                                                                    | public/                |
| 7   | Унифицировать **focus-visible:ring-2** у всех интерактивных элементов             | Компоненты             |
| 8   | Контраст тёмных секций (Reviews, Team, GiftSection, corp-контакты) — WCAG AA      | global.css, компоненты |
| 9   | Ошибки в консоли браузера — исправить                                             | JS-код                 |
| 10  | Устаревшие API — выявить и заменить                                               | JS-код                 |
| 11  | Сократить неиспользуемый CSS (Lighthouse)                                         | Tailwind, global.css   |
| 12  | Сократить неиспользуемый JS (Tourvisor/Nemo lazy load)                            | SearchWidget, виджеты  |
| 13  | **Cabinet:** Header или ссылка «На главную»; placeholder и type для input         | cabinet                |
| 14  | **Cabinet:** noindex для страницы входа; fieldset/legend; focus-visible на кнопке | cabinet                |

#### Желательно

| #   | Задача                                                                                                                                                    | Где                                                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| 15  | Блок FAQ (главная или corp)                                                                                                                               | Новая секция                                                    |
| 16  | SearchWidget табы: role="tablist", стрелки ← →                                                                                                            | SearchWidget.astro                                              |
| 17  | Бургер-меню: закрытие по Escape                                                                                                                           | Header.astro (проверить)                                        |
| 18  | truncate + title для обрезанного текста (Reviews, Team)                                                                                                   | Reviews, Team                                                   |
| 19  | Fallback шрифтов в font-family                                                                                                                            | global.css                                                      |
| 20  | preload LCP-картинки                                                                                                                                      | index.astro head                                                |
| 21  | Проверка на 320px                                                                                                                                         | Компоненты                                                      |
| 22  | Ссылки Footer «Политика» / «Соглашение» — убрать заглушки `#` или добавить страницы                                                                       | Footer                                                          |
| 23  | **Качество визуала (анти-ИИ-шаблон):** заменить `transition-all` на явные свойства (`transition-[transform,box-shadow]` и т.п.)                           | Компоненты, global.css (.glass-panel-interactive)               |
| 24  | **Качество визуала:** убрать дефолтные цвета Tailwind (indigo/blue/sky/emerald) из Hero по времени суток, Footer соцсетей, кнопок модалок — только @theme | Hero.astro, Footer.astro, ReviewModal, CallbackModal, GiftModal |
| 25  | **Качество визуала:** заменить оставшиеся `shadow-md` на двухслойные цветные тени (primary/cta с opacity)                                                 | cabinet, Header, Partners, About                                |
| 26  | **Качество визуала:** задать для body в base `line-height: 1.7`                                                                                           | global.css @layer base                                          |

### 5.3. Упущенные мелочи

- **Reviews:** `truncate` у подписей карточек — есть `title`, но проверить актуальность.
- **Partners:** класс `partners-shine` — в документе упомянут, в коде `partners-title`; проверить наличие.
- **Awards:** конфликты transition при hover — ранее были; при оптимизации заменены на `transition-[...]`.
- **Layout.astro:** canonical — нужно проверить, есть ли уже; og:url аналогично.

### 5.4. Порядок работы (рекомендуемый)

1. **Критично** (1–3) — og:image, Reviews placeholder, GiftModal Image.
2. **Важно: SEO** (4–6) — Schema.org, canonical, robots.txt.
3. **Важно: Lighthouse** (9–12) — CSS/JS, ошибки консоли, deprecated API.
4. **Важно: Доступность** (7, 14) — focus-visible, Cabinet.
5. **Важно: UI** (8) — контраст тёмных секций.
6. **Желательно** (15–22) — по мере возможностей.

---

## Часть 6. Тесты по веб-стандартам (рекомендации по приоритетам)

### 6.1. Критично (минимальный набор перед продакшеном)

| #   | Тест               | Инструмент                                                                           | Что проверяет                                                    |
| --- | ------------------ | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------- |
| 1   | **Lighthouse**     | Chrome DevTools (F12 → Lighthouse) или `npx lighthouse http://localhost:4321 --view` | Performance, Accessibility, Best Practices, SEO — главная и corp |
| 2   | **HTML Validator** | [validator.w3.org](https://validator.w3.org/)                                        | Корректность разметки — 2–3 страницы                             |
| 3   | **axe DevTools**   | Расширение Chrome «axe DevTools»                                                     | ARIA, контраст, фокус, семантика — базовая доступность           |

**Режимы:** Mobile (основной), Desktop; страницы: главная, `/corp`, `/cabinet`.

---

### 6.2. Важно

| #   | Тест                   | Инструмент                                                          | Что проверяет                                            |
| --- | ---------------------- | ------------------------------------------------------------------- | -------------------------------------------------------- |
| 4   | **CSS Validator**      | [jigsaw.w3.org/css-validator](https://jigsaw.w3.org/css-validator/) | Корректность CSS                                         |
| 5   | **WAVE**               | [wave.webaim.org](https://wave.webaim.org/)                         | Ошибки доступности, альтернатива axe                     |
| 6   | **Цветовой контраст**  | Chrome DevTools → Inspect → Contrast                                | Соответствие WCAG AA (4.5:1 для текста) в тёмных секциях |
| 7   | **PageSpeed Insights** | [pagespeed.web.dev](https://pagespeed.web.dev/)                     | Core Web Vitals на реальном URL (после деплоя)           |

---

### 6.3. Желательно (после деплоя)

| #   | Тест                         | Инструмент                                                                         | Что проверяет                                              |
| --- | ---------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| 8   | **WebPageTest**              | [webpagetest.org](https://www.webpagetest.org/)                                    | Скорость, водопад запросов, разбивка по регионам           |
| 9   | **Google Rich Results Test** | [search.google.com/test/rich-results](https://search.google.com/test/rich-results) | Schema.org, rich snippets (после добавления микроразметки) |
| 10  | **Яндекс Вебмастер**         | [webmaster.yandex.ru](https://webmaster.yandex.ru/)                                | Индексация, ошибки, регион — после деплоя                  |

---

### 6.4. Чек-лист перед релизом

- [ ] Lighthouse (Mobile) — главная, corp, cabinet — все категории ≥ 80 (или целевые значения)
- [ ] HTML Validator — главная и corp без критичных ошибок
- [ ] axe DevTools — 0 серьёзных нарушений
- [ ] Проверка контраста — тёмные секции (Reviews, Team, GiftSection, corp-контакты)
- [ ] PageSpeed Insights — на проде после деплоя

---

### 6.5. Рекомендуемый порядок

1. **Сейчас (preview):** Lighthouse, HTML Validator, axe DevTools (1–3).
2. **Перед деплоем:** CSS Validator, WAVE, контраст (4–6).
3. **После деплоя:** PageSpeed Insights, WebPageTest, Rich Results, Вебмастер (7–10).

---

### 6.6. Где проводить тесты: IDE, браузер или прод

Универсального «всё в IDE» нет — часть тестов требует браузер или задеплоенный сайт.

#### Из IDE (терминал / расширения)

| Тест               | Как запустить                                                                               |
| ------------------ | ------------------------------------------------------------------------------------------- |
| **Lighthouse**     | `npx lighthouse http://localhost:4321 --view` — headless Chrome, отчёт откроется в браузере |
| **HTML Validator** | Расширение VSCode (например, «HTML validation») или CLI `vnu`                               |
| **axe-core**       | Через npm + Playwright/Puppeteer — запуск из терминала                                      |

#### Через браузер

| Тест                      | Почему                                        |
| ------------------------- | --------------------------------------------- |
| **Lighthouse в DevTools** | Встроен в Chrome                              |
| **axe DevTools**          | Расширение Chrome                             |
| **Проверка контраста**    | DevTools → Inspect → Contrast                 |
| **WAVE**                  | Онлайн-сервис, нужен URL (localhost или прод) |

#### Только после деплоя (прод)

| Тест                   | Почему                                        |
| ---------------------- | --------------------------------------------- |
| **Яндекс.Вебмастер**   | Добавление сайта, проверка индексации, регион |
| **PageSpeed Insights** | Ориентирован на реальный URL и сеть           |
| **WebPageTest**        | Тест из разных регионов и устройств           |
| **Rich Results Test**  | Работает по URL (после добавления Schema.org) |

#### Кратко

- **IDE:** Lighthouse CLI, HTML/CSS Validator, axe через скрипты.
- **Браузер:** Lighthouse DevTools, axe, контраст, WAVE.
- **Прод:** Вебмастер, PageSpeed, WebPageTest, Rich Results.

---

## Часть 7. Качество визуала (анти-ИИ-шаблон)

По результатам аудита по статье [«Сайты от ИИ выглядят как сайты от ИИ»](https://habr.com/ru/articles/1004960/) (полный отчёт: `.doc/audit-ai-design-habr.md`) сайт в целом не выглядит как типичный ИИ-шаблон: есть брендовые цвета (@theme), парные шрифты (Montserrat + Inter), цветные тени и стиль Liquid Glass. Ниже — пункты, требующие доработки для соответствия рекомендациям статьи.

### 7.1. Соответствие критериям (кратко)

| Критерий                                                         | Статус | Действие                                                                                     |
| ---------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------- |
| Цвета только из @theme, без indigo/blue/sky/emerald по умолчанию | ⚠️     | Hero (градиенты по времени суток), Footer (соцсети), кнопки в модалках — перевести на токены |
| Тени многослойные, с оттенком бренда                             | ⚠️     | Убрать оставшиеся `shadow-md`; заменить на `shadow-xl shadow-primary/15` и аналоги           |
| Два шрифта (заголовки + тело)                                    | ✅     | Без изменений                                                                                |
| Body line-height 1.7                                             | ⚠️     | Задать в `global.css` в @layer base для body                                                 |
| Анимации: только transform и opacity, не `transition-all`        | ❌     | Заменить `transition-all` на явные свойства по всему проекту                                 |
| Hover / focus-visible / active у кликабельных                    | ✅     | Без изменений                                                                                |
| Система глубины (base / elevated / floating)                     | ⚠️     | Описать в конституции или гайде; в коде частично есть                                        |

### 7.2. Задачи по приоритету (включены в п. 5.2)

- **Важно (по мере рефакторинга):** п. 23 — замена `transition-all`; п. 24 — цвета только @theme; п. 25 — тени.
- **Желательно:** п. 26 — body line-height; при желании — формализация уровней глубины, опционально grain в hero.

Подробные обоснования и ссылки на файлы — в `.doc/audit-ai-design-habr.md`.

---

## Итог

- В документе собраны результаты анализа всего сайта (главная, **CORP**, **Cabinet**) и общие компоненты.
- **Часть 4** — автоматический аудит Lighthouse (27.02.2026): SEO 100, Accessibility 88, Performance 68, Best Practices 58.
- **Часть 5** — сводка: что сделано и что осталось; приоритеты и рекомендуемый порядок доработок.
- **Часть 6** — тесты по веб-стандартам: Lighthouse, валидаторы HTML/CSS, axe, WAVE, PageSpeed, Rich Results и др. — по приоритетам (критично / важно / желательно); чек-лист перед релизом.
- **Часть 7** — качество визуала (анти-ИИ-шаблон): соответствие критериям из аудита по статье Habr; задачи 23–26 в п. 5.2; полный отчёт в `.doc/audit-ai-design-habr.md`.
- Особый акцент — на мобильном отображении текста: длинные строки, CAPS, списки с flex, отсутствие переносов и `overflow-wrap`/`word-break`.
- **§ 9** описывает управление с клавиатуры: Skip Link, модалки (Escape, focus trap), `focus-visible`, табы SearchWidget, lightbox Reviews.
- **Часть 3** добавляет выводы из `.doc/dev_site.md` (тренды 2026: mobile first, SEO/E-E-A-T, GEO/AEO, микроразметка, аналитика) и сводную таблицу действий, объединяющую рекомендации анализа с этими трендами.
- Рекомендации сгруппированы по приоритетам (критично / важно / желательно) для планирования доработок.
