# ANRO TRIP — Полная переработка дизайна (2026)

**Версия:** 1.0  
**Дата:** 18 февраля 2026  
**Основа:** изучение lookup.design, Tailwind CSS, Material Design 3, Apple HIG, тренды веб-дизайна 2026 (Contented), Uprock, LandingFolio; конституция проекта и текущее руководство.

---

## 1. Сводка трендов из всех источников

### 1.1 lookup.design

- **Hero:** один сильный заголовок, одна CTA, иллюстрация или градиент, минимум текста, крупная типографика.
- **Карточки:** тень + скругления, внутренняя иерархия (заголовок → контент), одна CTA внутри.
- **Searchbar:** поиск как главный элемент, иконка лупы + очистка (×), теги/пилюли под полем.
- **Баннеры:** одна мысль, контраст текста к фону, одна кнопка, бейджи «NEW» при необходимости.
- **Footer:** тёмный фон, 4–5 колонок с заголовками, соцсети, один CTA (подписка/контент), копирайт внизу.
- **Navigation:** логотип слева, ссылки по центру/справа, одна выделенная CTA справа, минимализм.
- **Теги/чипы:** пилюли категорий под поиском для быстрого переключения (как направления).

### 1.2 Tailwind CSS

- Стили только утилитами, без лишнего CSS; JIT для произвольных значений.
- Showcase (NASA, Shopify, Vercel и др.) — уровень качества интерфейсов.
- Готовые блоки Tailwind Plus — референс для Hero, карточек, форм.

### 1.3 Material Design 3

- **Цвет:** палитра primary/secondary, контраст, темизация.
- **Типографика:** шкала размеров, иерархия, читаемость.
- **Motion:** Ripple, переходы (shared axis, fade), быстрая обратная связь.
- **Состояния:** hover, focus, active, disabled — явно заданы для интерактивных элементов.

### 1.4 Apple HIG (Liquid Glass)

- **Materials:** полупрозрачность, размытие, слои — «стекло» без перегруза.
- Чёткая иерархия, минимум визуального шума, аккуратное использование глубины.

### 1.5 Тренды веб-дизайна 2026 (Contented)

- **Минимализм с эмоциями:** мягкая анимация, типографика как акцент, реакция на действия.
- **Цвет и градиенты:** уход от «безопасных» палитр; тёплые и холодные вместе; градиенты с глубиной/шумом.
- **Микровзаимодействия:** кнопки, загрузка, реакция форм — интерфейс «общается».
- **Типографика:** контрастные пары шрифтов, нестандартные заголовки, крупный текст; читаемость в приоритете.
- **Сетки:** асимметрия, смещение блоков при сохранении логичной структуры.
- **Правило:** не перегружать; один тренд — качественно; тренд под проект и бренд.

### 1.6 Uprock

- Header/Footer: лучшие практики, что включать и чего избегать.
- Глассморфизм: характеристики, принципы.
- Дизайн-система: 10 обязательных компонентов, опора на документацию (Material, Spectrum).

### 1.7 LandingFolio

- Референсы лендингов по секциям (Hero, карточки, баннеры, футер).
- Компоненты под Tailwind — идеи структуры и стиля без копирования кода.

---

## 2. Текущее состояние проекта (кратко)

- **Цвета:** primary `#3fa0f0`, secondary `#1a6db1`, CTA `#ffd417` / hover `#e5be14`; Montserrat + Inter.
- **Структура:** Hero (sticky) → контентный блок (белый, `rounded-t-[40px]`) → SearchWidget → PopularTours → About → Partners → Reviews → Awards → GiftSection → Team → Contacts → Footer.
- **Уже есть:** fluid typography, glass-panel, staggered animations, micro-interactions (ripple, magnetic, shake), Container Queries, View Transitions, ScrollProgress, Ken Burns, динамические градиенты Hero.
- **Ограничения:** стилизация только через Tailwind (+ исключения в `global.css`), один `global.css`, без новых `.css` и `<style>` в компонентах (конституция).

---

## 3. Переработка по разделам

### 3.1 Цветовая система

**Принцип:** бренд не зафиксирован — можно выбрать **любую** трендовую палитру. Цвета ниже приведены по документам (Contented 2026, lookup.design, Material Design 3, Apple HIG, LandingFolio) как примеры; тон и оттенки можно менять под проект.

**Роли цветов (M3, lookup):**

- **Primary** — основной акцент: ссылки, иконки, выделение, навигация.
- **Secondary** — второй акцент: подзаголовки, второстепенные кнопки, теги.
- **Accent / CTA** — одна яркая кнопка (Hero, ключевые действия); контраст с фоном обязателен (WCAG).
- **Surface** — фон контента (часто белый или очень светлый нейтральный).
- **Footer** — тёмный тон для контраста с основным контентом (lookup, Campaignmonitor).

---

**Вариант A — Холодный + яркий акцент (lookup-стиль)**  
_Источник: lookup.design, Contented (градиенты, контраст)._

| Роль      | HEX / пример                   | Использование                     |
| --------- | ------------------------------ | --------------------------------- |
| Primary   | `#2563eb` (синий)              | Заголовки, ссылки, навигация      |
| Secondary | `#64748b` (slate)              | Подписи, второстепенные элементы  |
| CTA       | `#f59e0b` (янтарный)           | Главная кнопка Hero, ключевые CTA |
| Surface   | `#ffffff` / `#f8fafc`          | Фон контента                      |
| Footer bg | `#0f172a` (slate-900)          | Тёмный footer                     |
| Теги/чипы | `#e0e7ff` фон, `#3730a3` текст | Пилюли под поиском                |

---

**Вариант B — Тёплый + холодный (Contented 2026)**  
_«Тёплые и холодные оттенки в одном интерфейсе»._

| Роль      | HEX / пример                              | Использование         |
| --------- | ----------------------------------------- | --------------------- |
| Primary   | `#0d9488` (teal)                          | Акценты, ссылки       |
| Secondary | `#b45309` (amber-700)                     | Второй акцент, бейджи |
| CTA       | `#dc2626` (red-600)                       | Яркая CTA (энергия)   |
| Surface   | `#fffbeb` / `#fef3c7` (amber-50)          | Тёплый фон            |
| Footer bg | `#1c1917` (stone-900)                     | Тёмный footer         |
| Градиенты | `from-teal-500 via-amber-200 to-rose-400` | Hero, баннеры         |

---

**Вариант C — Глубокий премиум (Contented 2026)**  
_«Премиальность — глубокие тёмные палитры»._

| Роль          | HEX / пример           | Использование            |
| ------------- | ---------------------- | ------------------------ |
| Primary       | `#a78bfa` (violet-400) | Светлый акцент на тёмном |
| Secondary     | `#94a3b8` (slate-400)  | Подписи                  |
| CTA           | `#fbbf24` (amber-400)  | Кнопка на тёмном фоне    |
| Surface       | `#0f0f0f` / `#1e1e1e`  | Тёмный основной фон      |
| Hero/карточки | `#27272a` (zinc-800)   | Блоки на тёмном          |
| Footer bg     | `#09090b` (zinc-950)   | Ещё глубже               |

---

**Вариант D — Сочный градиентный (Contented 2026)**  
_«Насыщенные контрастные градиенты», «неожиданные переходы»._

| Роль          | HEX / пример                                  | Использование     |
| ------------- | --------------------------------------------- | ----------------- |
| Primary       | `#7c3aed` (violet-600)                        | Заголовки, ссылки |
| Secondary     | `#06b6d4` (cyan-500)                          | Второй акцент     |
| CTA           | `#22c55e` (green-500)                         | Яркая CTA         |
| Surface       | `#ffffff`                                     | Нейтральный фон   |
| Hero gradient | `from-violet-600 via-fuchsia-500 to-cyan-500` | Фон Hero          |
| Footer bg     | `#18181b` (zinc-900)                          | Тёмный footer     |

---

**Общие правила (из документов):**

- **Контраст:** текст на кнопках и фонах — по WCAG (минимум 4.5:1 для текста, 3:1 для крупного).
- **Один главный акцент:** не смешивать 3+ ярких цвета в одном блоке (Contented, lookup).
- **Градиенты:** допустимы с глубиной, лёгким шумом; не перегружать (Contented).
- **Реализация:** все цвета задавать в `@theme` в `global.css` (--color-primary, --color-secondary, --color-cta, --color-cta-hover, --color-footer-bg и т.д.) и использовать через Tailwind; при необходимости JIT: `bg-[#hex]`.

**Выбор палитры:** привязки к бренду нет — можно взять любой вариант A–D, смешать оттенки из разных вариантов или задать свою палитру по тем же ролям (primary, secondary, CTA, surface, footer), опираясь на тренды из документов.

---

### 3.2 Типографика

**Цель:** сложная типографика 2026 при сохранении читаемости; контраст пар (Montserrat + Inter).

| Элемент                 | Рекомендация                                                                                                                                                                   | Источник                |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------- |
| Базовый размер          | Оставить fluid: `clamp(14px, 0.875rem + 0.25vw, 16px)` в `html`.                                                                                                               | Текущий гайд, Contented |
| h1–h6                   | Оставить clamp-шкалу в `global.css`; Montserrat для заголовков.                                                                                                                | Текущий гайд            |
| Секционные подзаголовки | Ввести единый паттерн: маленький капслок над заголовком (например «ПОПУЛЯРНЫЕ НАПРАВЛЕНИЯ» / «ЧТО МЫ ПРЕДЛАГАЕМ») — как на lookup «WHAT WE OFFER». Цвет: primary или gray-600. | lookup                  |
| Тело текста             | Inter, достаточный line-height; в длинных блоках — `text-balance` где уместно.                                                                                                 | M3, Contented           |
| Кнопки и метки          | Montserrat, bold/extra-bold, uppercase где уже используется.                                                                                                                   | Текущий гайд            |

**Действия:**

- Добавить в дизайн-систему опциональный «секционный лейбл»: классы типа `text-xs font-bold uppercase tracking-widest text-primary` или `text-gray-600` для подзаголовка над h2.
- Не менять базовые h1–h6 в `global.css` без необходимости; все кастомизации — через утилиты в компонентах.

---

### 3.3 Hero

**Цель:** один фокус, одна CTA, современный вид (Liquid Glass, тренды 2026).

| Элемент      | Сейчас                                            | Рекомендация                                                                                    | Источник        |
| ------------ | ------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------- |
| Заголовок    | «Будь на высоте с ANRO TRIP!»                     | Оставить; при желании чуть укоротить подпись или усилить контраст фразы (один главный месседж). | lookup (hero)   |
| Подзаголовок | «Путешествия вашей мечты начинаются здесь»        | Оставить; один короткий текст.                                                                  | lookup          |
| CTA          | Одна кнопка «ПОИСК ТУРОВ»                         | Оставить одну кнопку; shine, 3% scale, glow — соответствуют трендам.                            | lookup, гайд    |
| Фон          | Ken Burns, динамический градиент по времени суток | Оставить; при желании чуть смягчить градиенты (лёгкая текстура/шум — по Contented).             | Contented, гайд |
| Стрелка вниз | animate-bounce                                    | Оставить; можно чуть приглушить или уменьшить размер для минимализма.                           | Apple HIG       |

**Действия:**

- Не дублировать CTA в Hero; не добавлять лишние блоки в первый экран.
- Проверить контраст текста на градиенте (белый + drop-shadow — ок).

---

### 3.4 Header / Navigation

**Цель:** минимализм, одна явная CTA, как на lookup / Segment / Campaignmonitor.

| Элемент        | Сейчас                                 | Рекомендация                                                                                                      | Источник           |
| -------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------ |
| Логотип        | Слева, + «ANRO TRIP»                   | Оставить; логотип + название.                                                                                     | lookup, navigation |
| Навигация      | О нас, Отзывы, Туры, Команда, Контакты | Оставить; при желании сократить до 4–5 пунктов; один выделенный пункт (например «Туры») можно визуально выделить. | lookup             |
| CTA в хедере   | Кнопка «Личный кабинет» / аналогично   | Одна кнопка справа (primary или контур); не две равнозначные.                                                     | lookup, navigation |
| Стекло         | `glass-panel`                          | Оставить; соответствует Liquid Glass.                                                                             | Apple HIG, гайд    |
| Мобильное меню | Скрыто до lg                           | Сохранить; бургер, выезжающая панель с теми же пунктами + одной CTA.                                              | M3, Uprock         |

**Действия:**

- Проверить, что в хедере только одна основная кнопка (не две конкурирующие).
- Все состояния ссылок и кнопок: hover/focus — явно (например `hover:text-primary`, `focus-visible:ring-2 focus-visible:ring-primary`).

---

### 3.5 Поиск (SearchWidget) и теги направлений

**Цель:** поиск как главный элемент после Hero; теги-категории как на lookup.

| Элемент          | Сейчас                                      | Рекомендация                                                                                                                                                                                                                                              | Источник           |
| ---------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| Контейнер        | glass-panel, табы Авиабилеты / Туры и отели | Оставить glass; поле поиска визуально доминирует.                                                                                                                                                                                                         | lookup (searchbar) |
| Под полем поиска | —                                           | Добавить **ряд тегов-направлений** (пилюли): Мальдивы, Дубай, Турция, Таиланд, Египет, Вьетнам и т.д. Клик — подстановка/фильтр или якорь к блоку туров. Стиль: `rounded-full` или `rounded-2xl`, фон primary/10 или neutral, текст primary или gray-800. | lookup (tags)      |
| Иконки           | В полях Nemo/Tourvisor                      | Лупа, очистка (×) — уже в виджетах; сохранить.                                                                                                                                                                                                            | lookup             |

**Действия:**

- Добавить компонент или блок «Теги направлений» под SearchWidget (или внутри него): 6–10 пилюль, единый ритм `px-4 sm:px-6`, max-w-7xl.
- Стиль пилюль: только Tailwind, без новых классов в `global.css` (например `px-4 py-2 rounded-full bg-primary/10 text-primary font-medium`).

---

### 3.6 Карточки (туры, услуги, партнёры)

**Цель:** карточки в духе lookup/M3 — тень, скругления, одна CTA, без лишних рамок.

| Элемент   | Рекомендация                                                                             | Источник            |
| --------- | ---------------------------------------------------------------------------------------- | ------------------- |
| Контейнер | `rounded-3xl`, тень (например `shadow-lg` или кастомная), без яркой обводки.             | lookup (card), гайд |
| Внутри    | Заголовок → описание/цена → одна кнопка/ссылка.                                          | lookup              |
| Hover     | Лёгкий подъём `hover:-translate-y-1`, усиление тени; уже есть в glass-panel-interactive. | M3, гайд            |
| Бейджи    | «Хит», «Luxury» и т.д. — компактно, один угол; не перегружать.                           | lookup (banner)     |

**Действия:**

- PopularTours: проверить единообразие скруглений и теней; убрать лишние границы, если появятся.
- About (услуги/преимущества), Partners, Awards — те же принципы: карточка = тень + rounded-3xl + одна внутренняя иерархия.

---

### 3.7 Баннеры и промо-блоки

**Цель:** одна мысль, контраст, одна CTA (как на lookup — Campaignmonitor, Voiceflow).

| Элемент              | Рекомендация                                                                                  | Источник        |
| -------------------- | --------------------------------------------------------------------------------------------- | --------------- |
| Акции / GiftSection  | Один заголовок, короткий текст, одна кнопка; фон — сплошной или градиент с контрастом текста. | lookup (banner) |
| Бейдж «NEW» / «Хит»  | Маленький, угол карточки или баннера; не перегружать.                                         | lookup          |
| Cookie/нижний баннер | Один месседж, одна кнопка «Принять» + ссылка «Подробнее» при необходимости.                   | lookup, M3      |

**Действия:**

- GiftSection, CookieBanner, любые новые промо — проверить по принципу «одна мысль + одна CTA».
- Цвета: primary/secondary/CTA или нейтральный фон + акцентная кнопка.

---

### 3.8 Footer

**Цель:** тёмный footer с колонками, соцсети, один CTA, копирайт (lookup, Campaignmonitor, Sentry).

| Элемент           | Сейчас                                                    | Рекомендация                                                                                                                                                                     | Источник        |
| ----------------- | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| Фон               | `#9ca3af` + нижняя полоса `bg-secondary`                  | **Тёмный единый блок:** например `bg-gray-900` или `bg-[#1a1d24]`; текст `text-white` / `text-gray-300`.                                                                         | lookup (footer) |
| Структура         | 2 колонки (оплата + реквизиты), потом copyright + соцсети | **4–5 колонок с заголовками:** например Направления (ссылки), Услуги, Контакты, Реквизиты, Оплата. Ниже — соцсети, один CTA («Подписаться» / «Заказать звонок»), затем копирайт. | lookup, Uprock  |
| Заголовки колонок | Uppercase, bold                                           | Оставить; цвет `text-white` или `text-gray-400`.                                                                                                                                 | lookup          |
| Ссылки            | —                                                         | `text-gray-300 hover:text-white`; без подчёркивания по умолчанию.                                                                                                                | M3              |
| CTA в footer      | Сейчас нет явного                                         | Один блок: подписка на рассылку или кнопка «Заказать звонок».                                                                                                                    | lookup          |

**Действия:**

- Переверстать Footer: один тёмный фон на весь блок; grid `md:grid-cols-4` или `md:grid-cols-5`; единый ритм `px-4 sm:px-6`, max-w-7xl.
- Нижняя полоса: копирайт + соцсети в одной строке (или два ряда на мобиле); убрать второй цвет полосы, оставить один тёмный тон.
- Все стили — Tailwind; при необходимости одна переменная в `@theme` для `--color-footer-bg`.

---

### 3.9 Секции контента (About, Partners, Reviews, Awards, GiftSection, Team, Contacts)

**Цель:** единый ритм, секционные подзаголовки, Bento/сетки где уместно.

| Элемент          | Рекомендация                                                                                                     | Источник                       |
| ---------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| Отступы          | Единый горизонтальный ритм `px-4 sm:px-6`, max-w-7xl; вертикальные отступы секций `py-12 md:py-16` или по гайду. | Текущий гайд, lookup           |
| Заголовки секций | Над h2 — маленький капслок (опционально): «О НАС», «ПОПУЛЯРНЫЕ НАПРАВЛЕНИЯ», «ПАРТНЁРЫ» и т.д.                   | lookup                         |
| Сетки            | Bento/нестандартные сетки — только где есть смысл (например PopularTours); остальное — ровные колонки.           | Contented, конституция (Bento) |
| TrustBadges      | Оставить в About; карточки-бейджи — без лишних рамок, скругления и тень.                                         | lookup (card)                  |

**Действия:**

- Ввести общий паттерн секции: опциональный лейбл (капслок) + h2 + контент.
- Не добавлять лишние декоративные полосы/рамки между секциями (уже убрана синяя полоса).

---

### 3.10 Glass / Materials (Liquid Glass)

**Цель:** единый стиль «стекла» по Apple HIG и гайду.

| Элемент                  | Рекомендация                                                                        | Источник          |
| ------------------------ | ----------------------------------------------------------------------------------- | ----------------- |
| .glass-panel             | Оставить в `global.css`; не дублировать в компонентах.                              | Конституция, гайд |
| .glass-panel-interactive | Hover: лёгкое усиление фона и тени, `translateY(-2px)`. Оставить.                   | Гайд, M3          |
| Header, SearchWidget     | Использовать существующие классы; не вводить новые варианты стекла без обоснования. | Apple HIG         |

**Действия:**

- Не создавать новые глобальные классы стекла без необходимости; при необходимости — только в `global.css` с комментарием (конституция V).

---

### 3.11 Микровзаимодействия и motion

**Цель:** эмоциональный UX 2026, быстрая обратная связь (M3, Contented).

| Элемент          | Сейчас                                  | Рекомендация                                                  | Источник        |
| ---------------- | --------------------------------------- | ------------------------------------------------------------- | --------------- |
| Кнопки           | ripple, magnetic                        | Оставить; при желании добавить ripple на другие основные CTA. | M3, гайд        |
| Формы            | shake при ошибке                        | Оставить.                                                     | M3              |
| Переходы страниц | View Transitions                        | Оставить.                                                     | Гайд            |
| Появление блоков | reveal-hidden / reveal-visible, stagger | Оставить; не увеличивать задержки.                            | Contented, гайд |
| ScrollProgress   | Градиентная полоса                      | Оставить.                                                     | Гайд            |

**Действия:**

- Убедиться, что у всех интерактивных элементов есть hover/focus (Tailwind: `hover:`, `focus-visible:`).
- Не добавлять тяжёлые анимации; микровзаимодействия — короткие (до 300 ms где возможно).

---

### 3.12 Сетки и отступы

**Цель:** единый ритм, при желании — лёгкая асимметрия (Contented).

| Элемент            | Рекомендация                                                                         | Источник          |
| ------------------ | ------------------------------------------------------------------------------------ | ----------------- |
| Контейнер контента | `max-w-7xl mx-auto px-4 sm:px-6` везде.                                              | Текущий гайд      |
| Секции             | `space-y-0` между блоками; вертикальные отступы внутри секций.                       | index.astro       |
| PopularTours       | Bento + Container Queries — оставить.                                                | Гайд, конституция |
| Смещение блоков    | Допустимо лёгкое смещение колонок или карточек для «движения»; не ломать читаемость. | Contented         |

**Действия:**

- Проверить все секции на единый `px-4 sm:px-6` и max-w-7xl.
- Не вводить новые значения отступов без причины (например, придерживаться 4/6/8/12/16/24).

---

### 3.13 Z-Index и модальные окна

**Цель:** не менять иерархию (гайд); модалки поверх всего.

| Уровень                       | Значение       | Использование                                          |
| ----------------------------- | -------------- | ------------------------------------------------------ |
| Модалки                       | z-[2147483647] | GiftModal, CallbackModal, ReviewModal, оверлеи Reviews |
| ScrollToTop                   | z-[2147483646] | Кнопка «Наверх»                                        |
| CookieBanner                  | z-[2147483640] | Нижний баннер                                          |
| OfficeWidget, FavoritesWidget | z-[2147483630] | Плавающие виджеты                                      |
| ScrollProgress                | z-9999         | Полоса прогресса                                       |
| Header                        | z-50           | Шапка                                                  |
| SearchWidget                  | z-20           | Форма поиска                                           |
| Контент                       | z-10           | Основной блок                                          |
| Hero                          | z-0            | Первый экран                                           |

**Действия:**

- Не менять порядок; при добавлении новых плавающих элементов — вписать в эту таблицу и использовать арбитражные значения в скобках: `z-[...]`.

---

## 4. Чек-лист внедрения

### Фаза 1 — Цвет и типографика (низкий риск)

- [ ] Ввести секционный лейбл (капслок над h2) в 2–3 секциях как пилот.
- [ ] Проверить контраст CTA и текста на Hero (WCAG).
- [ ] Добавить в документацию переменную для footer (при переходе на тёмный).

### Фаза 2 — Footer и баннеры

- [ ] Переверстать Footer: тёмный фон, 4–5 колонок, один CTA, копирайт + соцсети.
- [ ] Проверить CookieBanner и GiftSection на принцип «одна мысль + одна CTA».

### Фаза 3 — Поиск и теги

- [ ] Добавить блок тегов направлений под SearchWidget (пилюли).
- [ ] Связать теги с якорями или фильтрами (по возможности без поломки Tourvisor/Nemo).

### Фаза 4 — Карточки и секции

- [ ] Унифицировать карточки (PopularTours, About, Partners, Awards): тень, rounded-3xl, без лишних рамок.
- [ ] Везде, где есть h2 секции, опционально добавить лейбл сверху.

### Фаза 5 — Полировка

- [ ] Проверить все hover/focus у ссылок и кнопок.
- [ ] Пройти Lighthouse (LCP, доступность, контраст).
- [ ] Обновить гайд и этот документ после внедрения.

---

## 5. Ограничения (конституция)

- Стилизация **только** через Tailwind: утилиты → JIT → при невозможности — исключение в `global.css` с обоснованием.
- Не создавать новые `.css` файлы и не использовать `<style>` в компонентах для того, что можно сделать Tailwind.
- Единственный глобальный CSS: `src/styles/global.css`.
- Z-index не менять без необходимости; арбитражные значения — в квадратных скобках: `z-[2147483647]`.

---

## 6. Источники

| Источник                | URL                                                               | Использование                                             |
| ----------------------- | ----------------------------------------------------------------- | --------------------------------------------------------- |
| lookup.design           | https://lookup.design/                                            | Hero, card, searchbar, banner, footer, navigation, теги   |
| Tailwind CSS            | https://tailwindcss.com/                                          | Утилиты, showcase, Plus                                   |
| Material Design 3       | https://m3.material.io/                                           | Цвет, типографика, motion, состояния                      |
| Apple HIG               | https://developer.apple.com/design/human-interface-guidelines/    | Materials, Liquid Glass                                   |
| Тренды веб-дизайна 2026 | https://media.contented.ru/osnovy/napravleniya/trendy-web-design/ | Минимализм, цвет, микровзаимодействия, типографика, сетки |
| Uprock                  | https://www.uprock.ru/                                            | Header/footer, глассморфизм, дизайн-система               |
| LandingFolio            | https://www.landingfolio.com/inspiration                          | Референсы секций и компонентов                            |
| Конституция проекта     | `.specify/memory/constitution.md`                                 | Стилизация, global.css                                    |
| Руководство проекта     | `doc/anro-trip-guide-optimized.md`                                | Текущая дизайн-система, z-index, компоненты               |

---

**Конец документа.**

При внедрении изменений: сначала читать актуальный код и конституцию; не перезаписывать ручные правки; тестировать после каждой фазы.
