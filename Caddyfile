# Глобальные опции Caddy (официальный образ caddy:2-alpine, без xcaddy-плагинов).
# trusted_proxies — когда перед VPS стоит Cloudflare (DNS proxy / orange cloud).
# Диапазоны: https://www.cloudflare.com/ips-v4/ и https://www.cloudflare.com/ips-v6/
# Обновлять при изменении списка у Cloudflare (раз в квартал достаточно).
# Альтернатива с автообновлением: xcaddy + github.com/WeidiDeng/caddy-cloudflare-ip
{
	servers {
		trusted_proxies static \
			173.245.48.0/20 103.21.244.0/22 103.22.200.0/22 103.31.4.0/22 \
			141.101.64.0/18 108.162.192.0/18 190.93.240.0/20 188.114.96.0/20 \
			197.234.240.0/22 198.41.128.0/17 162.158.0.0/15 104.16.0.0/13 \
			104.24.0.0/14 172.64.0.0/13 131.0.72.0/22 \
			2400:cb00::/32 2606:4700::/32 2803:f800::/32 2405:b500::/32 \
			2405:8100::/32 2a06:98c0::/29 2c0f:f248::/32
		trusted_proxies_strict
		client_ip_headers CF-Connecting-IP X-Forwarded-For
	}
}

www.anrotrip.ru {
	redir https://anrotrip.ru{uri} permanent
}

anrotrip.ru {
	encode zstd gzip

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "camera=(), microphone=(), geolocation=()"
		-Server
	}

	# Хешированные ассеты Astro — длинный immutable cache
	@astroAssets path /_astro/*
	header @astroAssets Cache-Control "public, max-age=31536000, immutable"

	# Публичная статика с умеренным cache
	@publicStatic path /og-image.jpg /og-image.png /favicon.png /nemo/* /images/*
	header @publicStatic Cache-Control "public, max-age=604800"

	# Лимит тела запроса (формы /api/*; отзыв ≤ 8 KB)
	request_body {
		max_size 64KB
	}

	reverse_proxy app:4321
}
